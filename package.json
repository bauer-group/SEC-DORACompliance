{
  "name": "@bauer-group/dora-compliance",
  "version": "0.1.0",
  "description": "DORA Compliance Documentation – BAUER GROUP",
  "private": true,
  "license": "SEE LICENSE IN LICENSE",
  "type": "module",
  "scripts": {
    "setup": "npm install",
    "update": "npm update",
    "outdated": "npm outdated",
    "audit": "npm audit",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "docs:ci": "npm ci && vitepress build docs",
    "docs:clean": "rimraf docs/.vitepress/dist docs/.vitepress/cache node_modules/.vitepress",
    "docs:serve": "vitepress serve docs --port 4173",
    "lint": "markdownlint-cli2 \"docs/**/*.md\"",
    "lint:fix": "markdownlint-cli2 --fix \"docs/**/*.md\"",
    "check:links": "linkinator docs/.vitepress/dist --recurse --clean-urls --concurrency 10 --retry-errors --skip \"^https?://(?!(localhost|127.0.0.1))\"",
    "validate": "npm run lint && npm run docs:build && npm run check:links",
    "ci": "npm ci && npm run lint && npm run docs:build",
    "prepare": "husky"
  },
  "lint-staged": {
    "docs/**/*.md": "markdownlint-cli2 --fix"
  },
  "overrides": {
    "smol-toml": ">=1.7.1",
    "sharp": "^0.35.4"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "linkinator": "^8.0.2",
    "lint-staged": "^17.2.0",
    "markdownlint-cli2": "^0.23.2",
    "rimraf": "^6.1.3",
    "vitepress": "^1.6.4",
    "vitepress-plugin-rss": "^0.4.5"
  }
}
